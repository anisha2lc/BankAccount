const SIGN_IN = {
  INDEX: "/",
};

const CREATE_NEW_ACCOUNT = {
  INDEX: "/create-new-account",
};

const DEPOSIT_AMOUNT = {
  INDEX: "/deposit-amount",
};

const WITHDRAW_AMOUNT = {
  INDEX: "/withdraw-amount",
};

const BANK_BALANCE_INQUIRY = {
  INDEX: "/bank-balance-inquiry",
};

export {
  SIGN_IN,
  CREATE_NEW_ACCOUNT,
  DEPOSIT_AMOUNT,
  WITHDRAW_AMOUNT,
  BANK_BALANCE_INQUIRY,
};
